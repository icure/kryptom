headers = \
    openssl/aes.h \
    openssl/evp.h \
    openssl/err.h
# headerFilter = openssl/*
compilerOpts = -I/usr/include
linkerOpts = -L/usr/lib -lstdc++ -lm -lgcc_s -lgcc -lc -lgcc_s -lgcc -lcrypto
